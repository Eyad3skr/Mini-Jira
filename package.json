{
  "name": "mini-jira",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "dev:api": "npm run dev --prefix backend",
    "build": "npm run build --prefix frontend",
    "db:up": "docker compose up -d",
    "db:setup": "npm run create-tables --prefix backend && npm run seed --prefix backend",
    "setup": "npm install && npm install --prefix frontend && npm install --prefix backend && npm run db:up && sleep 2 && npm run db:setup"
  }
}
